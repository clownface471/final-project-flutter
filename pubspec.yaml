name: final_project_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  firebase_auth: ^6.0.2
  flutter:
    sdk: flutter
  google_fonts: ^6.3.1
  firebase_core: ^4.1.0
  http: ^1.5.0
  provider: ^6.1.5+1
  intl: ^0.20.2
  cloud_firestore: ^6.0.1
  flutter_staggered_animations: ^1.1.1
  phosphor_flutter: ^2.1.0
  go_router: ^14.2.0
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
